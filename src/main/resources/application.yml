logging:
  level:
    org:
      springframework:
        web: info
        security: info

spring:
  application:
    name: spring-security-social-sign-in
  datasource:
    url: jdbc:mysql://localhost:3306/oidc?createDatabaseIfNotExist=true
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:

